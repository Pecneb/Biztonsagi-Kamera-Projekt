import numpy
import matplotlib.pyplot as plt
import cv2

'''
With this module, we subtract the foreground mask (the moving object),
from the recieved frame.
'''
def get_foreground(bg, frame):
    fg
    return fg

if __name__ == "__main__":
    get_foreground()